# Weekly Report — Week of Monday 2026-07-06

> Engineering log for the e-commerce platform (Strangler Fig migration: monolith → microservices on AWS).
> One file per week. Each day: what I did, what I'm planning, and any problems and how I resolved them.

---

## Monday 2026-07-06

### What I did
No code changes committed today.

### What I'm planning
- Start the order-service Maven project: create the base directory structure, `pom.xml` with Spring Boot, Spring Data JPA, Flyway, PostgreSQL driver, and Spring Web — mirroring the product-service setup exactly.
- Write the V1 Flyway migration for order-service: `orders` and `order_items` tables replicating the monolith schema, so the new service can be shadow-validated before any traffic is cut over.
- Draft the domain layer: `Order` and `OrderItem` entities and the `IOrderRepository` port interface — no Spring or JPA imports, consistent with the hexagonal pattern used across the codebase.
- Add `/api/orders/**` to the gateway routing config with `ORDER_SERVICE_WEIGHT=0` as the initial env var, keeping all order traffic on the monolith until the new service passes end-to-end validation.

### Problems & solutions
None.

---

## Tuesday 2026-07-07

### What I did
No code changes committed today.

### What I'm planning
- Scaffold the order-service Maven project: base directory structure, `pom.xml` mirroring product-service (Spring Boot, Spring Data JPA, Flyway, PostgreSQL driver, Spring Web).
- Write the V1 Flyway migration for order-service: `orders` and `order_items` tables replicating the current monolith schema, to enable shadow validation before any traffic is shifted.
- Draft the hexagonal domain layer: `Order` and `OrderItem` entities plus the `IOrderRepository` port interface — no Spring or JPA imports, consistent with the pattern used across all other services.
- Wire `/api/orders/**` into the gateway routing config with `ORDER_SERVICE_WEIGHT=0` as the initial env var so all order traffic stays on the monolith until end-to-end validation passes.

### Problems & solutions
None.

---

## Wednesday 2026-07-08

### What I did
No code changes committed today.

### What I'm planning
- Begin implementing the order-service application layer: `OrderService` use-case class with `createOrder` and `getOrdersByCustomer` methods, translating between `OrderRequest`/`OrderResponse` DTOs and the domain entity — mirroring the structure already in place in the monolith.
- Write the `OrderMapper` to keep the domain entity from leaking into the HTTP layer, consistent with the DTO/Mapper pattern used in product-service.
- Draft the infrastructure layer: `OrderRepositoryAdapter` wrapping a Spring Data `OrderJpaRepository`, implementing the `IOrderRepository` port so the service layer never touches JPA directly.
- Confirm the V1 Flyway migration schema aligns with the monolith's current `orders` and `order_items` DDL — any drift here will cause shadow-validation mismatches once `ORDER_SERVICE_WEIGHT` is raised.

### Problems & solutions
None.

---

## Thursday 2026-07-09

### What I did
No code changes committed today.

### What I'm planning
- Implement the REST controller layer for order-service: `OrderController` with `POST /api/orders` and `GET /api/orders/customer/{customerId}` endpoints, completing the hexagonal adapter stack (domain → application → infrastructure → api).
- Wire up global exception handling in order-service: a `GlobalExceptionHandler` with `@RestControllerAdvice` returning a consistent `ApiResponse` shape, matching the pattern already in place in the monolith and product-service.
- Add a `ProductServiceClient` to order-service using Spring's `RestClient` to fetch product data at order-creation time, enabling the JSONB snapshot pattern to be replicated in the new service.
- Raise `ORDER_SERVICE_WEIGHT` in the gateway from 0 to a small value (5–10 %) once the controller layer passes local smoke tests, to begin shadow-validating live order traffic.

### Problems & solutions
None.

---

# Weekly Report — Week of Monday 2026-08-10

> Engineering log for the e-commerce platform (Strangler Fig migration: monolith → microservices on AWS).
> One file per week. Each day: what I did, what I'm planning, and any problems and how I resolved them.

---

## Monday 2026-08-10

### What I did
No code changes committed today.

### What I'm planning
- Scaffold the order-service Maven project: directory layout, `pom.xml` with Spring Boot, Spring Data JPA, Flyway, PostgreSQL driver, and Spring Web — mirroring the product-service structure.
- Write V1 Flyway migration for order-service: `orders` and `order_items` tables replicating the monolith schema so the service can be shadow-validated before any traffic shifts.
- Draft the hexagonal domain layer for order-service: `Order` and `OrderItem` entities plus the `IOrderRepository` port interface with no Spring or JPA imports — consistent with the pattern across all existing services.
- Port `OrderProductSnapshot` serialization logic into order-service before any traffic can move, since this snapshot currently lives entirely in the monolith.
- Add `/api/orders/**` to the gateway routing config with `ORDER_SERVICE_WEIGHT=0` as the initial state so all order traffic stays on the monolith until the new service passes end-to-end validation.

### Problems & solutions
None.

---

## Tuesday 2026-08-11

### What I did
No code changes committed today.

### What I'm planning
- Scaffold the order-service Maven project with `pom.xml`, directory layout, and the same dependency set as product-service (Spring Boot, Spring Data JPA, Flyway, PostgreSQL driver, Spring Web).
- Write V1 Flyway migration for order-service: `orders` and `order_items` tables, matching the monolith schema so the new service can be shadow-validated before any traffic shifts.
- Draft the hexagonal domain layer: `Order` and `OrderItem` entities plus the `IOrderRepository` port interface with no Spring or JPA imports.
- Port `OrderProductSnapshot` serialization into order-service before any traffic moves, since this snapshot logic currently lives entirely in the monolith.
- Add `/api/orders/**` to the gateway routing config with `ORDER_SERVICE_WEIGHT=0` as the initial state, keeping all order traffic on the monolith until the new service passes end-to-end validation.

### Problems & solutions
None.

---

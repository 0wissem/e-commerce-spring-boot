# Weekly Report — Week of Monday 2026-06-29

> Engineering log for the e-commerce platform (Strangler Fig migration: monolith → microservices on AWS).
> One file per week. Each day: what I did, what I'm planning, and any problems and how I resolved them.

---

## Monday 2026-06-29

### What I did
No code changes committed today.

### What I'm planning
- Kick off the order-service extraction: scaffold the Spring Boot project with hexagonal architecture (domain → application → infrastructure → api), following the same package structure as product-service.
- Write the V1 Flyway migration for order-service: create the `orders` and `order_items` tables from scratch; the monolith keeps its own tables until traffic cutover is validated, then a V23 monolith migration drops them.
- Add the `/api/orders/**` route to the gateway config alongside the existing product-service routes — set initial weight to 0 so all order traffic continues hitting the monolith until the new service is live and tested.
- Wire up the GitHub Actions CI pipeline for order-service from day one so every commit triggers a build and ECR push automatically.

### Problems & solutions
None.

---

## Tuesday 2026-06-30

### What I did
No code changes committed today.

### What I'm planning
- Begin scaffolding order-service: create the Maven project, configure `pom.xml` with Spring Boot, Spring Data JPA, Flyway, and PostgreSQL driver — mirroring the product-service setup.
- Draft V1 Flyway migration for order-service: `orders` and `order_items` tables with the same schema as the monolith, so the new service can be validated against identical data shapes before any traffic is shifted.
- Decide on the HTTP client approach for order-service to call product-service when fetching product snapshots at order creation — likely reuse the `RestClient`-based pattern already in the monolith's `ProductServiceClient`.
- Review the gateway routing config to confirm `/api/orders/**` can be added with weight=0 without disrupting the existing product-service routes.

### Problems & solutions
None.

---

## Wednesday 2026-07-01

### What I did
No code changes committed today.

### What I'm planning
- Start the order-service Maven project in earnest: set up `pom.xml`, base package structure (`domain/`, `application/`, `infrastructure/`, `api/`), and `application.properties` with dev (H2) and prod (PostgreSQL) profiles.
- Write V1 Flyway migration for order-service: `orders` and `order_items` tables, matching the monolith schema so the new service can be shadow-tested before any traffic is cut over.
- Add the `/api/orders/**` route to the gateway with an initial `ORDER_SERVICE_WEIGHT=0` env var, following the same weighted-routing pattern used for product-service.
- Pin the GitHub Actions CI pipeline for order-service to build and push to ECR on every commit to the new service directory.

### Problems & solutions
None.

---

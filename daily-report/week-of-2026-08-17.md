# Weekly Report — Week of Monday 2026-08-17

> Engineering log for the e-commerce platform (Strangler Fig migration: monolith → microservices on AWS).
> One file per week. Each day: what I did, what I'm planning, and any problems and how I resolved them.

---

## Monday 2026-08-17

### What I did
No code changes committed today.

### What I'm planning
- Begin scaffolding the order-service Maven project: `pom.xml`, directory layout, and the same dependency set as product-service (Spring Boot, Spring Data JPA, Flyway, PostgreSQL driver, Spring Web).
- Write the V1 Flyway migration for order-service: `orders` and `order_items` tables replicating the monolith schema exactly so the new service can be shadow-validated before any live traffic moves.
- Draft the hexagonal domain layer for order-service: `Order` and `OrderItem` entities plus the `IOrderRepository` port interface with no Spring or JPA imports — consistent with patterns in the monolith and product-service.
- Port the `OrderProductSnapshot` serialization logic into order-service before touching the gateway weight; this snapshot currently lives entirely in the monolith and must be present in the new service first.
- Add `/api/orders/**` to the gateway routing config with `ORDER_SERVICE_WEIGHT=0` as the initial state, keeping all order traffic on the monolith until end-to-end validation passes.

### Problems & solutions
None.

---

## Tuesday 2026-08-18

### What I did
No code changes committed today.

### What I'm planning
- Continue with the order-service extraction plan laid out yesterday: scaffold the Maven project and write the V1 Flyway migration for `orders` and `order_items`.
- Draft the hexagonal domain layer (entities + `IOrderRepository` port) before any infrastructure or API code is written, consistent with the pattern established in product-service.
- Set up the `OrderProductSnapshot` serialization in order-service so cross-service product data is handled correctly from day one.
- Add the gateway routing rule for `/api/orders/**` with `ORDER_SERVICE_WEIGHT=0` as the initial safe default.

### Problems & solutions
None.

---

## Wednesday 2026-08-19

### What I did
No code changes committed today.

### What I'm planning
- Start the actual scaffolding work for order-service: create the Maven project structure, `pom.xml`, and top-level package layout mirroring product-service.
- Write the V1 Flyway migration (`V1__create_orders_tables.sql`) for order-service with `orders` and `order_items` tables matching the monolith schema, so the new service can be validated against existing data before any traffic shifts.
- Draft the hexagonal domain layer: `Order` and `OrderItem` entities and the `IOrderRepository` port interface — no Spring or JPA imports in the domain layer, consistent with the pattern in both the monolith and product-service.
- Port `OrderProductSnapshot` serialization logic into order-service before touching gateway weights so cross-service product data is handled correctly from day one.
- Add the `/api/orders/**` routing rule to the gateway config with `ORDER_SERVICE_WEIGHT=0` as the safe initial state.

### Problems & solutions
None.

---

## Thursday 2026-08-20

### What I did
No code changes committed today.

### What I'm planning
- Begin the actual order-service scaffold: create the Maven project with `pom.xml` and directory layout mirroring product-service (`domain/`, `application/`, `infrastructure/`, `api/`).
- Write `V1__create_orders_tables.sql` for order-service Flyway, replicating the `orders` and `order_items` schema currently owned by the monolith — prerequisite for shadow-validation before any traffic shift.
- Draft the hexagonal domain layer: `Order` and `OrderItem` entities plus the `IOrderRepository` port interface with zero Spring or JPA imports, consistent with the approach used in both the monolith and product-service.
- Port `OrderProductSnapshot` serialization into order-service before touching gateway weights so cross-service product data ownership is solved correctly from day one.
- Add the `/api/orders/**` route to the gateway config with `ORDER_SERVICE_WEIGHT=0` as the safe default, keeping all order traffic on the monolith until end-to-end validation completes.

### Problems & solutions
None.

---

## Friday 2026-08-21

### What I did
No code changes committed today.

### What I'm planning
- Use next week to break the planning deadlock and start shipping: the order-service scaffold has been planned across five consecutive days without a single commit — next milestone is a working Maven project on disk with `pom.xml`, package layout, and the V1 Flyway migration before end of Monday.
- Write `V1__create_orders_tables.sql` for order-service, replicating the `orders` and `order_items` schema from the monolith — this is the concrete first deliverable gating everything else.
- Draft the domain layer (`Order`, `OrderItem`, `IOrderRepository`) in the same session so the hexagonal skeleton is complete before moving to infrastructure.
- Port `OrderProductSnapshot` into order-service and wire it to the application service layer before any gateway weight change.
- Add the `/api/orders/**` route to the gateway with `ORDER_SERVICE_WEIGHT=0` so the plumbing is in place and weight promotion to 1 % can be tested immediately once the service passes local validation.

### Problems & solutions
None.

---

# Weekly Report — Week of Monday 2026-06-22

> Engineering log for the e-commerce platform (Strangler Fig migration: monolith → microservices on AWS).
> One file per week. Each day: what I did, what I'm planning, and any problems and how I resolved them.

---

## Monday 2026-06-22

### What I did
No code changes committed today.

### What I'm planning
- Continue the order-service extraction — the preprod environment is ready to validate each step before it reaches production.
- Investigate the pre-existing full-text search error on production product-service; this was the original performance bottleneck that drove the extraction and should be in a working state.

### Problems & solutions
None.

---

## Tuesday 2026-06-23

### What I did
No code changes committed today.

### What I'm planning
- Begin order-service extraction: scaffold the new Spring Boot service with hexagonal architecture (domain → application → infrastructure → api layers), mirroring the product-service structure.
- Set up a dedicated PostgreSQL RDS instance for order-service in the preprod environment.
- Define the Flyway baseline migration (V1) for the order-service schema, copying the relevant table definitions from the monolith.

### Problems & solutions
None.

---

## Wednesday 2026-06-24

### What I did
No code changes committed today.

### What I'm planning
- Scaffold the order-service Spring Boot project with the hexagonal architecture layers (domain → application → infrastructure → api), following the same package structure as product-service.
- Write the V1 Flyway migration for order-service: create the `orders` and `order_items` tables, migrating the schema currently owned by the monolith.
- Register order-service in the GitHub Actions CI pipeline so the build and ECR push are automated from day one.

### Problems & solutions
None.

---

## Thursday 2026-06-25

### What I did
No code changes committed today.

### What I'm planning
- Finalize the order-service scaffold design: confirm the package layout, identify which domain entities and value objects need to move (Order, OrderItem, OrderStatus), and decide the API contract before writing any code.
- Plan the Flyway migration strategy for order-service: V1 creates the schema from scratch; the monolith keeps its tables until traffic cutover is validated, then a V23 migration drops them from the monolith side.
- Review the gateway routing config to plan how `/api/orders/**` will be added once order-service is live — same pattern as the product-service route.

### Problems & solutions
None.

---

## Friday 2026-06-26

### What I did
No code changes committed today.

### What I'm planning
- Start the order-service extraction next week: create the Spring Boot project skeleton with hexagonal layers, write V1 Flyway migration (orders + order_items tables), and open the GitHub Actions pipeline so CI runs from the first commit.
- Add the `/api/orders/**` route in the gateway config alongside the existing product-service routes — keeping it at weight=0 initially so all traffic still hits the monolith until the new service is validated.
- Decide whether order-service needs its own `ProductServiceClient` or whether order items will rely entirely on the existing snapshot stored in the monolith's order_items table during the transition.

### Problems & solutions
None.
